import { HilightMouseDirective } from './hilight-mouse.directive';

describe('HilightMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new HilightMouseDirective();
    expect(directive).toBeTruthy();
  });
});
